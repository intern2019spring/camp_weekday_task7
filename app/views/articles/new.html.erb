<h2>新規論文投稿</h2>
<%= form_with model: @article, local: true do |f| %>
  <div>
    <p><%= f.label "論文タイトル" %></p>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <p><%= f.label "本文" %></p>
    <%= f.text_area :body %>
  </div>
  <%= f.submit "投稿" %>
<% end %>